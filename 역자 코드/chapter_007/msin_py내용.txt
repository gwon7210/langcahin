# -*- coding: utf-8 -*-

# 파일을 실행시키면 main.py 의 한글이 깨집니다
# 이 파일을 참고하세요

import streamlit as st


def init_page():
    st.set_page_config(
        page_title="Ask My PDF(s)",
        page_icon="🧐"
    )


def main():
    init_page()

    st.sidebar.success("👆의 메뉴에서 선택해주세요)

    st.markdown(
    """
    ### Ask My PDF(s)에 어서오세요!

    - 이 애플리케이션에서는 업로드한 PDF에 대해 질문을 할 수 있습니다
    - 먼저 왼쪽 메뉴에서 📄 Upload PDF(s)를 선택해서 PDF를 업로드해 주세요
    - PDF를 업로드 한 후에는 🧐 PDF QA를 선택해서 질문해 보세요😇
    )

if __name__ == '__main__':
    main()
	
	



